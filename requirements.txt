# Core dependencies for circulaire_service.py
# Compatible with Odoo 15 (Debian 11, Python 3.9)

requests>=2.28.0,<3.0
pdfplumber>=0.9.0,<1.0
pytesseract>=0.3.10,<1.0
Pillow>=9.0.0,<11.0
opencv-python-headless>=4.7.0,<4.10
numpy>=1.21.0,<2.0

# System packages (run in Docker):
# apt-get update && apt-get install -y tesseract-ocr tesseract-ocr-ara tesseract-ocr-fra poppler-utils

# Test: python3 circulaire_service.py -i 1 -y 25